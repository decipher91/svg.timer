/**
 * Created by decipher on 2.11.16.
 */
$(function () {
    /*$('.timer-days').svgTimer({
        direction: 'backwards',
        time: 35,
        interval: 86400000,
        hint: 'days'
    });

    $('.timer-hours').svgTimer({
        direction: 'backwards',
        time: 18,
        interval: 3600000,
        hint: 'hours'
    });

    $('.timer-minutes').svgTimer({
        direction: 'backwards',
        time: 54,
        interval: 60000,
        hint: 'minutes'
    });

    $('.timer-seconds').svgTimer({
        direction: 'backwards',
        time: 60,
        interval: 1000,
        hint: 'seconds'
    });*/
    $('.timer').svgCountDown({

    });
});